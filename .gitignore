# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
claude/
zymo/
# Base de datos SQLite
*.db
*.db-shm
*.db-wal
data/

# Variables de entorno (jamás secrets en el índice; si aparecen: git rm --cached -- archivo)
/backend/.env
/frontend/.env
.env
.env.*
!.env.example
# Excepción: .env.production del frontend solo contiene la URL pública del backend (sin secretos)
!frontend/.env.production

# Node / Frontend
node_modules/
dist/
.cache/

# Claude Code worktrees y configuración local — .claude/ ya NO se ignora completo
# (git no permite reincluir archivos dentro de un directorio ignorado por patrón),
# así que se ignora puntualmente lo que debe quedar local. .claude/skills/ sí se
# versiona a propósito, para que cualquiera que clone el repo tenga las skills.
.claude/worktrees/
.claude/settings.local.json
.claude/roles-y-permisos.md
.worktrees/

# Editores
.vscode/
.idea/
*.swp
*.swo

# Obsidian (configuración local del vault)
.obsidian/

# Archivos adjuntos / binarios locales
plans/archivos/
Imagenes/

# OS
.DS_Store
Thumbs.db

# Docs de implementación (uso interno multi-agente, no van al repo)
docs/implementacion/

# Documentación interna / estrategia de negocio
Master_plan/
Excalidraw
taskflow/
auditoria Gestión de tareas v2/
# Guía de configuración del servidor (contiene rutas e instrucciones internas)
SERVIDOR_CONFIGURACION.md
.superpowers/
